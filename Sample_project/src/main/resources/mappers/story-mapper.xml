<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.sample.story">
		
	<!-- story write -->
	<insert id="insertStory">
		insert into tbl_story(st_no, user_id, st_content)
		values(seq_story_no.nextval, #{user_id}, #{st_content})
	</insert>
	
	<!-- story list -->
	<select id="listStory" resultType="StoryVo">
		select * from tbl_story
		order by st_no desc
	</select>
	
	<!-- story delete -->
	<delete id="deleteStory">
		delete from tbl_story
		where st_no = #{st_no}
	</delete>
	
	<!-- story update -->
	<update id="updateStory">
		update tbl_story set
			st_content = #{st_content},
			mode_date = sysdate
		where st_no = #{st_no}
	</update>
	
	<!-- 글조회 -->
	<select id="selectStory" resultType="StoryVo">
		select * from tbl_story
		where st_no = #{st_no}
	</select>
	
</mapper>







